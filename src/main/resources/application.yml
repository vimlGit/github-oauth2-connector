spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23libEwiGkBtGKU5dl
            client-secret: 9827fed0fde4517cfcd972dde952312069211cfd
            scope:
              - repo
              - user
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user